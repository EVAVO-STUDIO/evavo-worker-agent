name: Stage atomic Worker package identity

on:
  push:
    branches:
      - work/package-identity-atomic-20260727
    paths:
      - ".github/workflows/stage-atomic-package-identity.yml"
      - "scripts/stage-atomic-package-identity.mjs"
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: stage-atomic-worker-package-identity
  cancel-in-progress: false

jobs:
  stage:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    timeout-minutes: 8
    steps:
      - name: Checkout staging branch
        uses: actions/checkout@08eba0b27e820071cde6df949e0beb9ba4906955 # v4.3.0
        with:
          ref: work/package-identity-atomic-20260727
          persist-credentials: true

      - name: Set up Node.js 24
        uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4.4.0
        with:
          node-version: "24"

      - name: Build the aligned package identity tree
        run: node scripts/stage-atomic-package-identity.mjs

      - name: Commit the staged tree
        shell: bash
        run: |
          set -euo pipefail
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          if git diff --cached --quiet; then
            echo "No package identity changes were required."
            exit 0
          fi
          git commit -m "Stage aligned Worker package identity"
          git push origin HEAD:work/package-identity-atomic-20260727
